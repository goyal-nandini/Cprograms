// gets() puts()
#include<stdio.h>
void main ()
{
    char name [0];    // what is this [] me kuch bhi number type karoo orr use follow na karoo then same is printing why ??
    printf("type a string:");
    gets(name);
    printf("the string you typed is:");
    puts(name);
}
    