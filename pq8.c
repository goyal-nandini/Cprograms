// checking for natural numbers.
#include <stdio.h>
int main ()
{
    int number;
    printf("enter number \n");
    scanf("%d",&number);
    
    if (number >=1){
        printf("the number is natural.");}
        else {
            printf("the number is not natural.");
        }
        
        
}