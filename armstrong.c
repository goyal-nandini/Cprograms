// NOT COMPLETED ....

// to check if a number is armstrong number or not.
/*armsrtong number is a number that is equal to the sum of the cubes of its own digits.*/

#include<stdio.h>
#include<math.h>
int main ()
{   int number , a,b,c;
    printf("enter any number : \n");
    scanf("%d",&number);

    if (number == pow(a,3)+pow(b,3)+pow(c,3) )
    {
        printf("the number is armstrong. \n");
    } else {
        printf("the number is not a armstrong number. \n");
    }

}